#include<stdio.h>
int main()
{
 int Max(int x,int y);
 int a,b,c;
mh/*
 scanf("%d,%d",&a,&b);

 c=Max(a,b);
 printf("The max is %d\n",c);
 return 0;
*/
}
int Max(int x,int y)
{
  int z;
  if(x>y)z=x;
  else z=y;
  return (z);
}
